@model IEnumerable<Asiservy.Automatizacion.Datos.Datos.CC_VERIFICACION_POTENCIOMETRO>


<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Modelo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Serie)
        </th>
        <th>
            NaCI 1%
        </th>
        <th>
            NaCI 2%
        </th>
        <th>
            NaCI 3%
        </th>
        <th>
            Observación
        </th>
        <th>
            Estado
        </th>

        <th>
           Creado
        </th>
        <th>
            Aprobado
        </th>

    </tr>

@foreach (var item in Model) {
    <tr style="font-size:11px">
        <td>
            @item.Fecha.ToString("dd-MM-yyyy")
        </td>
        <td style="white-space:normal">
            @Html.DisplayFor(modelItem => item.Modelo)
        </td>
        <td style="white-space:normal">
            @Html.DisplayFor(modelItem => item.Serie)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NaCI1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NaCI2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NaCI3)
        </td>
        <td style="white-space:normal">
            @Html.DisplayFor(modelItem => item.Observacion)
        </td>
        @if (item.EstadoReporte == true)
        {
            <td><span class="badge badge-success">Aprobado</span></td>
        }
        else
        {
            <td><span class="badge badge-danger">Pendiente</span></td>
        }

            <td>
                @Html.DisplayFor(modelItem => item.UsuarioIngresoLog) <br />
                @item.FechaIngresoLog.ToString("dd-MM-yyyy HH:mm")
            </td>
        <td style="font-size:10px">

            @if (item.FechaAprobacion != null)
            {
                @Html.DisplayFor(modelItem => item.AprobadoPor) <br />
                @item.FechaAprobacion.Value.ToString("dd-MM-yyyy HH:mm")
            }
        </td>

    </tr>
}

</table>
